export enum FONT_SIZE {
  XS = '12px',
  SM = '14px',
  MD = '16px',
  LG = '18px',
  XL = '24px',
  XXL = '30px',
}

export enum FONT_WEIGHT {
  REGULAR = 400,
  MEDIUM = 500,
  SEMIBOLD = 600,
  BOLD = 700,
}
